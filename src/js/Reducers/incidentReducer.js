import * as types from '../Actions/actionTypes';

export function incidentReducer(state = {
}, action) {
    switch (action.type) {
        default:
            return state;
            break;
    }
}

export default incidentReducer;